"""Impact analysis engine and reporting.

Orchestrates the hybrid retrieval pipeline: graph traversal finds structural
dependencies, semantic search finds conceptual similarities, and the LLM
synthesizes both into human-readable risk explanations.
"""
